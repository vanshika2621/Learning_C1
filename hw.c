#include <stdio.h>

int main() {
    // printf is a function from the standard input/output library (stdio.h)
    printf("Hello, World!\n");

    // main function should return 0 to indicate successful execution
    return 0;
}