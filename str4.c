#include <stdio.h>

int main() {
    // Declare and initialize a string (a character array)
    char myString[] = "Hello, C Programming!";

    // Print the string
    printf("The string is: %s\n", myString);

    return 0;
}